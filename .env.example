# Used by docker compose (copy to .env in repo root)
JWT_SECRET=replace_with_your_own_long_random_string_at_least_32_chars
JWT_EXPIRES_IN=7d

# Public URLs / ports
CLIENT_URL=http://localhost:8080
CLIENT_PORT=8080
CLIENT_DEV_PORT=5173
API_PORT=5050
MONGO_PORT=27017
